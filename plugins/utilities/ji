export const run = {
   usage: ['bot'],
   category: 'utilities',
   async: async (m, {
      client,
      text,
      isPrefix,
      command,
      Utils,
   }) => {
      client.sendSticker(m.chat, '/lib/media/image/empire.webp', '' m)
   },
   error: false
}

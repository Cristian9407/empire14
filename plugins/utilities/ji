export const run = {
   usage: ['bot'],
   category: 'utilities',
   async: async (m, {
      client,
      Utils
   }) => {
      client.sendSticker(m.chat, '/lib/media/image/empire.webp', '' m)
   },
   error: false
}
